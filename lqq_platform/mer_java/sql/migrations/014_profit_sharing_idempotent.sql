-- ============================================================
-- [LQQ-迁移] 014: 分账幂等保护 — 增加唯一索引防止重复分账记录
-- 日期: 2026-05-08
-- 说明: 同一订单的同一接收方类型+用户只能有一条分账记录
-- ============================================================

-- 添加唯一索引防止重复分账记录
ALTER TABLE `eb_wechat_profit_sharing_record`
    ADD UNIQUE INDEX `uk_order_receiver` (`order_no`, `receiver_type`, `receiver_user_id`);
