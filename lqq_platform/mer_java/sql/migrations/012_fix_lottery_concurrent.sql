-- ============================================================
-- [LQQ-迁移] 012: 修复抽奖并发安全 — 增加唯一索引防止同期重复参与
-- 日期: 2026-05-08
-- 说明: 同一用户在同一活动的同一期只能参与一次
-- ============================================================

-- 添加唯一索引防止并发重复参与
ALTER TABLE `eb_lottery_record`
    ADD UNIQUE INDEX `uk_activity_uid_period` (`activity_id`, `uid`, `period_number`);
